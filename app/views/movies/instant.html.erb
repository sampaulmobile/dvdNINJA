

<% if @torrents %>
  <h1> ADDED: </h1>

  <%= @torrents.each do |t| %>
    <li><%= t %></li>
  <% end %>

<% end %>


<%= form_tag instant_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :phrase, params[:phrase] %>
    <%= submit_tag "Add", :name => nil %>
  </p>
<% end %>


<%= link_to "Update RT", update_rt_path %>

<%= link_to "Torrent RT", torrent_rt_path %>
